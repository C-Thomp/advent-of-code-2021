package Day_4
import Day_4.Input._
object GiantSquid {

//def markNumber(, call:Int)={
//  if (board.contains(call.toString)) scanBoard

  def main(args: Array[String]): Unit = {
    testBoards.map(_.map(println(_)))

  }
}
